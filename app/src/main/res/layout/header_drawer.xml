<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:background="#017a84">

    <ImageView
        android:id="@+id/header_drawer_imageview"
        android:layout_width="70dp"
        android:layout_height="65dp"
        android:layout_marginTop="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_logo" />

    <TextView
        android:id="@+id/header_drawer_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Stélio Moiane"
        android:textColor="#fff"
        android:textSize="8pt"
        android:textStyle="bold|italic"
        app:layout_constraintEnd_toEndOf="@+id/header_drawer_imageview"
        app:layout_constraintStart_toStartOf="@+id/header_drawer_imageview"
        app:layout_constraintTop_toBottomOf="@+id/header_drawer_imageview" />

</android.support.constraint.ConstraintLayout>